#Will Bohmann
# 7.2 Lists Even Elements only
# Print even elements only from a list
# if each number is divisble by two and can equal zero then it works

data = [int(s) for s in input().split()]
for numbers in data:
    if (numbers % 2 == 0):
      print(numbers, end=" ")
